<br>
<%if @lists.size > 0%>
  <%@lists.each do |list|%>
  <p><%=list.name%></p>
  <%end%>
<%else%>
  <h3 class="text-center">You don't have lists yet</h3>
<%end%>

<a href=<%=new_user_list_path(current_user)%> class='text-center'><p>create new list</p></a>